#
# cachercize-config.cmake.in
#

set(CACHERCIZE_VERSION @CACHERCIZE_VERSION@)

@PACKAGE_INIT@

set_and_check(CACHERCIZE_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

check_required_components(cachercize)

#include (CMakeFindDependencyMacro)
#find_dependency (PkgConfig)
#pkg_check_modules (margo REQUIRED margo)

include ("${CMAKE_CURRENT_LIST_DIR}/cachercize-targets.cmake")
